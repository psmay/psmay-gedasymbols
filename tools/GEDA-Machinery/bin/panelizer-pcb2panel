#!/usr/bin/perl

eval 'exec /usr/bin/perl  -S $0 ${1+"$@"}'
    if 0; # not running under some shell

use warnings;
use strict;
use Carp;
use 5.010;

use GEDA::Machinery::PCB::Panelizer;
GEDA::Machinery::PCB::Panelizer->pcb2panel_run("panelizer-pcb2panel", @ARGV);

